# 日本留学新闻雷达配置说明

## 当前配置

本项目已针对中文日本留学内容创作进行定制：

- 每次抓取最近 48 小时的信息
- 输出语言为简体中文
- 每期最多保留 20 条高价值内容
- 重点覆盖 EJU/JLPT、留学生入试与募集要项、政策签证与奖学金、生活与就业
- 优先保留含有新规则、日期、数字、资格条件和选题冲突的信息
- GitHub Actions 默认每天日本时间 06:00 自动运行

## 第一次运行前必须完成

1. 登录 DeepSeek 开放平台并创建 API Key。
2. 打开项目根目录下的 `.env` 文件。
3. 将密钥填写在 `DEEPSEEK_API_KEY=` 后面，不要添加引号或空格。
4. 双击 `启动日本留学新闻雷达.cmd`，或在终端中运行：

   ```bat
   启动日本留学新闻雷达.cmd
   ```

生成的 Markdown 简报位于 `data/summaries/`。

## GitHub 每日自动运行

自动运行需要把当前定制版本放到你自己的 GitHub 仓库，并在仓库中配置：

- Secret 名称：`DEEPSEEK_API_KEY`
- Secret 值：你的 DeepSeek API Key
- GitHub Pages 来源：`gh-pages` 分支

工作流文件已经配置在 `.github/workflows/daily-summary.yml`。首次推送并添加 Secret 后，可在 GitHub 的 Actions 页面手动运行一次 `Daily Horizon Summary` 验证。

## 可选推送

如需将结果推送到飞书、Discord 或其他 Webhook：

1. 在 `.env` 中填写 `HORIZON_WEBHOOK_URL`。
2. 在 `data/config.json` 和 `data/config.github.json` 中将 `webhook.enabled` 改为 `true`。
3. 按目标平台调整 `platform` 和消息模板。

密钥只应保存在本地 `.env` 或 GitHub Secrets 中，不要写入可提交的 JSON 文件。
